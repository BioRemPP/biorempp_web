# ============================================================================
# UC-2.3: Ranking of Compounds by Sample Diversity per Chemical Class
# ============================================================================
# Description: Vertical bar chart showing compounds ranked by unique sample count
# Module: module2
# Plot Type: Bar Chart (Vertical)
# Rendering: On-demand (dropdown selection)
# ============================================================================

# ============================================================================
# SECTION 1: Metadata (REQUIRED)
# ============================================================================
metadata:
  use_case_id: "UC-2.3"
  module: "module2"
  title: "Ranking of Compounds by Sample Diversity per Chemical Class"
  description: >
    Vertical bar chart visualization showing chemical compounds ranked by
    the number of unique biological samples that interact with them,
    filterable by compound class. Identifies broadly degraded compounds.
  version: "1.0.0"
  author: "BioRemPP Development Team"
  created_date: "2025-12-01"
  tags:
    - "sample_counts"
    - "compound_ranking"
    - "chemical_class"
    - "bar_chart"
    - "vertical"
  plot_type: "bar_chart"
  scientific_context:
    domain: "Chemical Ecology"
    application: "Compound bioavailability and degradation pathway analysis"
    interpretation: "Compounds with higher sample counts represent common substrates with broad degradation pathways"

# ============================================================================
# SECTION 2: Data Configuration (REQUIRED)
# ============================================================================
data:
  source: "biorempp-merged-data"
  source_type: "store"
  
  required_columns:
    - "compound"
    - "sample_count"
  
  processing:
    steps:
      - name: "validate"
        enabled: true
        validator: "standard"
        description: "Validate required columns exist"
      
      - name: "sort"
        enabled: true
        params:
          by: "sample_count"
          ascending: false  # Descending for vertical bar chart
        description: "Sort by sample count (descending)"

# ============================================================================
# SECTION 3: Visualization Configuration (REQUIRED)
# ============================================================================
visualization:
  strategy: "BarChartStrategy"
  
  plotly:
    chart_type: "bar"
    x: "compound"
    y: "sample_count"
    orientation: "v"  # Vertical orientation
    
    # Text on bars
    text: "sample_count"
    text_position: "auto"
    
    # Color configuration
    color_discrete_sequence: ["mediumseagreen"]
    
    # Title configuration
    title:
      show: true  # Toggle title visibility
      text: "Ranking of Compounds by Sample Interaction"
      font:
        size: 18
        family: "Arial, sans-serif"
        color: "#2c3e50"
      x: 0.5
      xanchor: "center"
    
    # Axis labels
    labels:
      compound: "Compound Name"
      sample_count: "Number of Unique Samples"
    
    # Layout configuration
    layout:
      height: 600
      autosize: true  # Enable responsive sizing
      template: "simple_white"
      
      # X-axis configuration
      xaxis:
        title:
          text: "Compound Name"
          font:
            size: 14
            color: "#2c3e50"
        tickfont:
          size: 10
        tickangle: -45  # X-axis label angle (rotated for readability)
      
      # Y-axis configuration
      yaxis:
        title:
          text: "Number of Unique Samples"
          font:
            size: 14
            color: "#2c3e50"
        tickfont:
          size: 11
        tickangle: 0  # Y-axis label angle (horizontal)
      
      # Margins
      margin:
        l: 80
        r: 40
        t: 80
        b: 140  # Extra bottom margin for rotated labels
      
      # Hover configuration
      hovermode: "closest"
      hoverlabel:
        bgcolor: "white"
        font_size: 12
        font_family: "Arial"

# ============================================================================
# SECTION 4: Interactivity Configuration (REQUIRED)
# ============================================================================
interactivity:
  triggers:
    - component_id: "uc-2-3-class-dropdown"
      property: "value"
      trigger_type: "dropdown_selection"
      description: "Render chart when compound class is selected"
  
  outputs:
    - component_id: "uc-2-3-chart-container"
      property: "children"
      output_type: "dcc.Graph"
      description: "Compound diversity bar chart"
  
  states:
    - component_id: "merged-result-store"
      property: "data"
      description: "Merged data from all databases"

# ============================================================================
# SECTION 5: Validation Configuration (REQUIRED)
# ============================================================================
validation:
  rules:
    - rule: "not_empty"
      message: "Input data cannot be empty"
    
    - rule: "required_columns"
      columns: ["compound", "sample_count"]
      message: "Data must contain 'compound' and 'sample_count' columns for ranking analysis"
    
    - rule: "minimum_samples"
      min_count: 1
      message: "At least 1 compound required for ranking"
    
    - rule: "no_nulls"
      columns: ["compound", "sample_count"]
      message: "Null values not allowed in required columns"

# ============================================================================
# SECTION 6: Performance Configuration (REQUIRED)
# ============================================================================
performance:
  cache:
    enabled: true
    layers:
      - layer: "dataframe"
        ttl: 7200
        key_template: "uc_2_3_df_{data_hash}_{filters_hash}"
        description: "Cache compound ranking per class"
      
      - layer: "graph"
        ttl: 3600
        key_template: "uc_2_3_bar_{data_hash}_{filters_hash}"
        description: "Cache generated bar chart"
    
    invalidation:
      on_data_change: true
      on_config_change: true
      on_filter_change: true
  
  logging:
    enabled: true
    level: "INFO"
    log_cache_hits: true
    log_generation_time: true

# ============================================================================
# SECTION 7: Error Handling (RECOMMENDED)
# ============================================================================
error_handling:
  validation_errors:
    missing_columns:
      action: "display_message"
      message: "Required columns missing: {columns}. Compound ranking requires 'compound' and 'sample_count' columns."
    
    empty_dataframe:
      action: "display_placeholder"
      message: "No compound data available for selected class."
    
    insufficient_samples:
      action: "display_message"
      message: "At least 1 compound required for ranking. Found: {count}"
    
    no_class_selected:
      action: "display_placeholder"
      message: "Please select a compound class from the dropdown."
  
  processing_errors:
    aggregation_failure:
      action: "log_and_notify"
      fallback: "display_error_message"
      message: "Failed to calculate compound rankings: {error_details}"
    
    plot_render_failure:
      action: "log_and_notify"
      fallback: "display_error_message"
      message: "Failed to render bar chart: {error_details}"

# ============================================================================
# NOTES: Flexible Properties & Data Flow
# ============================================================================
# Flexible Properties (Configurable via YAML):
# - orientation: "v" (vertical) or "h" (horizontal)
# - title.show: true/false (toggle title visibility)
# - title.text: Chart title text
# - xaxis.tickangle: X-axis label rotation angle (-45 for readability)
# - yaxis.tickangle: Y-axis label rotation angle (0 = horizontal)
# - xaxis.title.text: X-axis title
# - yaxis.title.text: Y-axis title
# - color_discrete_sequence: Bar colors
# - layout.autosize: true/false (responsive sizing)
# - layout.height: Chart height in pixels
# - text: Column to display on bars
# - text_position: "auto" (automatic text positioning)
#
# Data Processing Flow:
# 
# CALLBACK (uc_2_3_callbacks.py):
# 1. Extract data from BioRemPP store
# 2. Map column names (Compound_Class/compoundclass, Compound_Name/compoundname, Sample/sample)
# 3. Filter by selected compound class from dropdown
# 4. GroupBy 'compoundname' and count unique samples → creates 'sample_count'
# 5. Rename 'compoundname' to 'compound'
# 6. Sort descending by sample_count
# 7. Pass to PlotService
#
# STRATEGY (BarChartStrategy):
# 1. Receives pre-aggregated data with ['compound', 'sample_count']
# 2. Validates columns exist
# 3. Sorts by 'sample_count' descending (for vertical display)
# 4. Creates vertical bar chart with flexible properties
#
# Database Support:
# - BioRemPP: ✅ Contains 'compoundclass' column
# - KEGG: ❌ Does NOT contain compound class data
#
# Rendering Logic:
# - Dropdown selection: Render chart for selected chemical class
# - No auto-update (single trigger: dropdown value change)
#
# Orientation Details:
# - Vertical (orientation: "v")
# - X-axis: compound (categorical labels, rotated -45°)
# - Y-axis: sample_count (numeric values)
# - Sorted descending for better visual hierarchy
